{
  "version": 2,
  "installCommand": "cd front && npm install",
  "buildCommand": "cd front && npm run build",
  "outputDirectory": "front/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}